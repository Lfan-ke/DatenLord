# 模块的泛型、端口、参数及约束

<!-- By Leo Cheng -->

|参数类型|关键字|说明|示例|
|:--:|:--:|:--:|:--:|
|数值参数|Integer|整数常量|Integer depth = 16|
|类型参数|type t|数据类型|type t|
|数值类型参数|numeric type n|编译时常量|numeric type n|

```
module mkModuleName#(参数)(端口，默认Empty，即：没有端口); // 没有端口也得必须写，哪怕写个空括号
endmodule
```

在这里不使用`泛型`这个表述，使用`参数化`来代指，一个语言习惯...

## 参数示例



## 端口类型



## 类型约束

Provisos，


**其他补充**：

```bsv
Integer n = 8;           // 运行时值，不能用于类型
numeric type n = 8;      // 编译时常量，可用于类型
```
